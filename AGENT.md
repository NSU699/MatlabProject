# CLAUDE.md — MATLAB 专题实践项目（数值计算方向）

本文件仅适用于本仓库。请在每次会话开始时通读一遍，并在整个协作过程中遵守。

## 一、项目定位

这是一门课程的期末大作业仓库。最终交付物是一份可提交、可答辩的完整成果，而不是零散的脚本片段。

- **选题**：第七类「数值计算问题」，含第 15 题与第 16 题。
  - 第 15 题：一维泊松问题 `d²u/dx² = exp(-x²)`，边界条件 `u(-1) = u(1) = 0`，`x ∈ [-1, 1]`，用**有限差分法**求数值解。
  - 第 16 题：一阶常微分方程初值问题 `dy/dt = -y + t² + 3`，`y(0) = 1`，`t ∈ [0, 3]`，用**四阶龙格库塔法**求数值解，同时用**符号计算**求解析解，并对二者进行比较。
- **团队规模**：1 人。不要在报告或代码中编造团队成员、分工表或多人协作记录。凡模板中要求「团队成员分工」的位置，如实按单人填写。
- **截止时间**：2026 年 10 月 15 日。
- **提交内容**：结题报告（docx）、源程序、成果资料、汇报 PPT、5–10 分钟解说视频。
- **评分要点**：成果深度与创新性、工作量与难度、表述能力、文档与程序规范程度。**一旦发现抄袭按不及格处理**——所有代码与文字必须原创。

由于题目本身的计算量偏小，本项目的竞争力来自**深度**而非广度：误差分析、收敛阶验证、稳定性讨论、不同格式/步长的对比、与解析解的定量比对，这些才是拿分点。凡有机会把一个结论做扎实，就不要停留在「跑出一条曲线」。

## 二、目录结构

```
MatlabProject/
├── AGENT.md               本文件
├── runAll.m               一键运行入口
├── src/                   MATLAB 源代码
│   ├── poisson/           第 15 题：有限差分求解泊松问题
│   ├── ode/               第 16 题：RK4 与符号解对比
│   ├── common/            两题共用的工具函数（绘图样式、误差度量、导出等）
│   └── tests/              公共层与算法验收测试
├── results/               脚本运行产生的数据（.mat / .csv）
├── figures/               脚本导出的图片（.png + .pdf/.eps 各一份）
├── 报告/                  结题报告，基于学校提供的 docx 模板撰写
├── ppt/                   汇报 PPT 与讲稿
└── tmp/                   临时中间文件，不属于交付物，不要在报告中引用
```

新建文件时按此结构归位。`tmp/` 下已有的内容是工具产生的中间物，不要当作项目资料。

## 三、MATLAB 代码规范

**语言**：所有注释、变量说明、图表标签、命令行输出一律使用**中文**（数学符号与单位除外）。代码标识符本身用英文。

**文件头**：每个 `.m` 文件开头必须有注释块，说明功能、输入、输出、作者、日期、以及它对应报告中的哪一节或哪张图。例如：

```matlab
function [x, u] = solvePoissonFD(N)
% solvePoissonFD  用二阶中心差分法求解一维泊松问题
%
%   求解 d^2u/dx^2 = exp(-x^2)，边界条件 u(-1) = u(1) = 0
%
%   输入:
%       N - 区间等分数，内部节点数为 N-1
%   输出:
%       x - 节点坐标列向量，长度 N+1
%       u - 对应的数值解列向量
%
%   对应报告: 第 3.2 节，图 3-1
%   作者: <姓名>   日期: 2026-09-08
```

**命名**：函数与脚本用小驼峰（`solvePoissonFD`、`compareRK4Exact`）；变量用有意义的英文小驼峰（`stepSize`、`errMax`），禁止 `a`、`tmp1`、`data2` 这类无信息命名；常量用全大写下划线。脚本文件以 `run` 或 `demo` 前缀标识入口（`runPoissonConvergence.m`）。

**结构**：算法核心必须写成函数，放在 `src/` 对应子目录；脚本只负责组织参数、调用函数、出图、存结果。禁止把算法逻辑直接铺在脚本里。

**可运行性**：每个入口脚本必须能独立运行——开头 `clear; clc; close all;`，自行 `addpath` 到所需目录，不依赖前一个脚本残留的工作区变量。

**可复现性**：任何用到随机数的地方必须显式 `rng(42)` 固定种子，并在注释中写明。参数（步长、网格数、容差）集中定义在脚本顶部的参数区，不要散落在代码中间。

**健壮性**：对外部输入的函数要做参数校验（`validateattributes` 或显式判断），并给出中文报错信息。

## 四、绝对禁止臆造数据与结果

这是本项目最重要的一条约束。

- 报告、PPT、对话中出现的**每一个数值、每一张图、每一张表**，都必须来自本仓库脚本的实际运行输出。
- 不允许「估计」误差量级、「大概」收敛阶、凭经验写一个看起来合理的数字。
- 每张图、每张表都要注明生成它的脚本文件名与运行日期，写在图注或表注里，也写在脚本的文件头注释中。
- 数值结果先存到 `results/` 下的 `.mat` 或 `.csv`，报告从这些文件里取数，不要从终端里手抄。
- 如果某个结果还没跑出来，就明确说「尚未计算」，绝不用占位数字填空。占位符必须写成 `TODO`，不能写成一个像模像样的数。
- 引用文献或公式时，若无法确认出处，说明不确定，不要虚构参考文献条目、页码或作者。

## 五、图表与论文格式统一

**图表**：所有绘图通过 `src/common/` 里的统一样式函数产生，保证字体、字号、线宽、配色一致。

- 图窗尺寸统一，中文字体使用宋体或黑体并在代码中显式设置，避免导出后中文变方框。
- 坐标轴必须有中文标签与单位；多曲线必须有图例；对比图统一用 `LineWidth = 1.5`。
- 误差随步长变化的图使用双对数坐标（`loglog`），并在图上标注理论收敛阶的参考斜率线。
- 每张图同时导出**位图**（`.png`，300 dpi，供 PPT 用）与**矢量图**（`.pdf`，供报告用），文件名与生成脚本对应，如 `figures/poisson_convergence.png`。
- 图片编号按报告章节走，格式为「图 3-1」「表 4-2」。

**报告**：严格基于 `报告/专题项目实践报告模板-XXX-XXX(团队成员姓名)-2026.docx` 的结构撰写，不擅自增删一级章节，不改动模板的样式定义。正文用连贯的中文段落论述，公式用公式编辑器格式并右侧编号，避免通篇罗列要点符号。文件名按模板要求替换为真实学号与姓名。

## 六、协作方式

**每步先给方案再动手。** 在下列任一情况发生前，先用文字说明打算怎么做、为什么这么做、会新增或改动哪些文件，等我确认后再执行：

- 新建脚本或函数文件
- 对已有算法实现做结构性修改
- 决定数值方法的关键设计（差分格式、网格策略、误差度量方式、步长序列）
- 开始撰写或大幅改写报告章节

小幅修补（改注释错别字、调图例位置、修一个明显的 bug）可以直接做，做完说明改了什么。

方案要具体到可执行的程度：说清楚函数签名、输入输出、验证方式，而不是「我来实现一下有限差分」这种笼统描述。同时指出你考虑过但放弃的替代方案及理由——我需要看到取舍，这也是报告里「难点与创新」一节的素材。

**验证习惯**：写完算法先做正确性验证再谈结果。第 15 题可用制造解（method of manufactured solutions）验证二阶收敛；第 16 题有解析解可直接比对。验证不通过就不要往下推进，更不要在报告里展示未经验证的结果。

**沟通风格**：用中文，直接给结论和判断，不要过度铺陈。发现我的思路有问题时直接指出，不要顺着我说。
